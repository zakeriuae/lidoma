{
  "name": "lidoma",
  "version": "1.0.0",
  "description": "پلتفرم جامع لید مارکتینگ کلینیک‌ها",
  "scripts": {
    "dev": "npx tailwindcss -i css/tailwind-input.css -o css/tailwind-built.css --minify && npx http-server . -p 5000 -o",
    "start": "npx http-server . -p 5000 -o",
    "tailwind:watch": "npx tailwindcss -i css/tailwind-input.css -o css/tailwind-built.css --watch",
    "build": "npx tailwindcss -i css/tailwind-input.css -o css/tailwind-built.css --minify && node build.js"
  },
  "keywords": [
    "lidoma",
    "lead-marketing",
    "clinic"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
